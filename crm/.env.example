# Copy to .env.local for local dev. On Vercel, set these in Project Settings →
# Environment Variables (root = crm/). Both values are publishable/public-safe
# (they ship in the client bundle); RLS guards the data.
NEXT_PUBLIC_SUPABASE_URL=https://xwsmovmtfymiqvgjicfk.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_V3JmyojHfTThe3odVgbbZQ_wCrhO3kD

# --- AI model (optional) ----------------------------------------------------
# No model is wired by default — the app runs its deterministic grounded
# fallback. To enable a model, set the Azure OpenAI vars below (the Google /
# Gemini transports were removed after the hackathon):
#   AZURE_OPENAI_ENDPOINT / AZURE_OPENAI_API_KEY / AZURE_OPENAI_DEPLOYMENT
#   AZURE_OPENAI_API_VERSION   # optional, default 2024-08-01-preview
