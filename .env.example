# Copy to .env and fill in. Never commit .env.
#
# Notion internal integration token (starts with ntn_...).
# Create at https://www.notion.so/profile/integrations -> New integration (Internal).
# Then open EACH "🟢 Prompt inbox" page in Notion -> ••• -> Connections -> connect this integration.
NOTION_TOKEN=ntn_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
