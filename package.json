{
  "name": "sandelcode-orchestrator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Notion -> local Claude session orchestrator for the HMD CRM hackathon team.",
  "scripts": {
    "start": "node --env-file=.env watcher/poll.mjs",
    "watch": "node --env-file=.env watcher/poll.mjs"
  }
}
